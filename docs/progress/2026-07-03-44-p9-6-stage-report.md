# P9-6 stage report / paper draft

## 当前目标

P9-6 的目标是把 P9-5 已经完成的 24-program depth1-only 总结写成一份可展示、可答辩、可继续扩展成论文草稿的阶段报告。该阶段不新增实验、不新增 certificate、不新增 search，不运行 runtime benchmark，也不修改 PassSpec。

## 完成内容

1. 新增 `docs/ecpor_stage_report.md`，作为 P9-6 阶段报告 / 论文草稿入口。
2. 更新 `README.md`，把阶段报告加入项目首页入口，并把下一步更新为 P10 PassSpec provenance v2。
3. 更新 `docs/ecpor_mvp_report.md`，把 P9-6 阶段报告和 stage report manifest 加入主要文件列表。
4. 新增 `docs/results/stage_report_manifest.json`，记录阶段报告、README、MVP 主报告和 P9-5 summary manifest 的 hash。
5. 更新 `docs/project_progress.md`，把最新状态推进到 P9-6，并新增本进度文件索引。

## 阶段报告结构

`docs/ecpor_stage_report.md` 按如下结构组织：

```text
摘要
研究问题
核心思想
方法链路
证据等级
结果总览
Objective Layer
Attribution Cases
解释
限制
下一步
```

报告的核心叙事是：

```text
ECPOR 不是完整 pass-order searcher；
它是 evidence-carrying LLVM phase-ordering reduction 原型。
先用 state-indexed AB/BA certificate 证明哪些相邻顺序不用搜，
再把 not-certified 的小空间交给 bounded local validation 和 objective-layer 检查。
```

## 核心结果

P9-6 沿用 P9-5 的 post-MVP 24-program depth1-only 总结：

```text
BenchmarkSets = 3
TotalPrograms = 24
TotalPairMatrixCertificates = 672
TotalReproducedCertificates = 672
TotalHardFalseIndependent = 0
TotalAdjacentAttempts = 168
CertifiedAdjacentEvents = 101
NotCertifiedAdjacentEvents = 39
TotalOneSwapCandidates = 39
TotalBothSmallerPrograms = 2
Diverse8BothSmallerPrograms = 0
AttributionCases = 2
Depth1Only = True
NoNewExperiments = True
```

解释：证书层和 reduction 链路稳定，但 objective-layer benefit 很稀疏。Diverse8 增加覆盖面后没有新增 depth1 both-smaller program，因此当前不建议继续 two-swap、depth=3、beam/searcher 或 runtime benchmark。

## 关键文档快照

阶段报告摘要片段：

```markdown
ECPOR 当前不是完整 pass-order searcher，也不宣称找到全局最优 pass 顺序。
它的目标更窄：在给定 LLVM IR state、给定 scalar pass 集合和给定相邻 pass pair 的条件下，
用可复现的 AB/BA certificate 证明哪些相邻顺序可以折叠，哪些顺序必须保留为候选，
再把剩余的小空间交给 bounded local validation 与 objective-layer 检查。
```

证据等级片段：

```markdown
| evidence | 含义 | 是否 hard prune |
| --- | --- | --- |
| `certified_independent_event` | 当前 state 中 AB/BA hard hash 相同，且 certificate 可复现 | yes |
| `not_certified_event` | 当前 state 中 AB/BA hard hash 不同 | no |
| `low_priority` | static filter 认为优先级较低，但未动态证明 | no |
| `one_swap_objective_result` | one-swap candidate 的 `.text` 观察结果 | no |
| `attribution` | 单个 observed case 的 feature/opcode/object 解释 | no |
```

manifest scope 片段：

```json
{
  "stage": "P9-6",
  "summary_only": true,
  "new_experiments": false,
  "new_certificates": false,
  "new_search": false,
  "runtime_benchmarks": false,
  "two_swap_search": false,
  "paper_draft": true,
  "passspec_provenance_changes": false
}
```

## 生成 manifest

`docs/results/stage_report_manifest.json` 在 clean commit `ca5842d294d34660898e2dfb80a67a4e7d933b35` 上生成，记录：

```text
stage = P9-6
result_generated_from_commit = ca5842d294d34660898e2dfb80a67a4e7d933b35
ecpor_git_dirty = false
summary_only = true
new_experiments = false
new_certificates = false
new_search = false
runtime_benchmarks = false
two_swap_search = false
```

## 验证结果

运行命令：

```powershell
D:\Miniconda\envs\dlm\python.exe -m pytest -q
```

目标结果：

```text
117 passed
```

同时使用 manifest 验收脚本确认：

```text
Stage=True
DirtyFalse=True
SummaryOnly=True
NoNewExperiments=True
NoNewCertificates=True
NoNewSearch=True
NoRuntime=True
NoTwoSwap=True
PaperDraft=True
StageReportWritten=True
```

## 风险和边界

1. P9-6 是文档收束阶段，不是新实验阶段。
2. P9-6 不修改 `passspec.yaml`；PassSpec provenance 升级留到 P10。
3. P9-6 不改变 `v0.1.1` MVP 定义；`v0.1.1` 仍是 Stanford-8 + Misc8 的 16-program / 448-certificate 发布边界。
4. P9-6 不把 P7b two-swap 结果写成 depth1 结论；24-program 总结仍然遵循 P9-5 depth1-only 口径。

## 下一步

建议进入 P10 PassSpec provenance v2：给 `may_produce`、`may_consume`、`requires_any` 等 hint 增加来源、置信度和 empirical support，并保持 loader 兼容旧 schema。当前仍不建议继续 two-swap、depth=3、beam/searcher、runtime benchmark、Alive2 或 PassInstrumentation。
